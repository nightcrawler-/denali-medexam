<%= form_with(model: examination_session, local: true) do |form| %>
  <% if examination_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(examination_session.errors.count, "error") %> prohibited this examination_session from being saved:</h2>

      <ul>
        <% examination_session.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date_of_exam %>
    <%= form.date_select :date_of_exam %>
  </div>

  <div class="field">
    <%= form.label :examination_type %>
    <%= form.text_field :examination_type %>
  </div>

  <div class="field">
    <%= form.label :workplace_id %>
    <%= form.text_field :workplace_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
